[build]
builder = "NIXPACKS"

[deploy]
startCommand = "streamlit run streamlit_app_acbl_elo_ratings.py --server.port $PORT --server.address 0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/"
