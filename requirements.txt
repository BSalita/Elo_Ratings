# Requirements for FFBridge/ACBL Elo Ratings Project
# ===================================================

# Core Streamlit web interface
# Watch AgGrid visually: >=1.56 historically blanked some grids; confirm in UI.
streamlit
streamlit-aggrid==1.1.8.post1
streamlit-extras
python-dotenv

# HTTP requests for API calls
requests
fastapi
uvicorn

# Data processing and analysis
pandas
polars
duckdb
psycopg[binary]
# The deployment image clones the published mlBridge repository. Its package
# initializer imports mlBridgeLib and mlBridgeAcblLib eagerly, so these remain
# required until the lazy initializer is published there.
scikit-learn
sqlalchemy
sqlalchemy-utils

# Data visualization
matplotlib
seaborn

# PDF generation for reports
reportlab
markdown
beautifulsoup4

# Standard library extensions (if needed)
typing-extensions
psutil

# Development and debugging
IPython

# Optional: Bridge analysis library
endplay

# Optional ML dependencies (uncomment if using mlBridgeAiLib.py)
# torch
# tqdm

# Note: The DDS library (dds.py) requires external DLL files
# Make sure dds.dll is available in the specified path